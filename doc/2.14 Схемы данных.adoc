=== Схемы данных

Ниже приведена логическая схема ключевых сущностей TeamBridge (упрощенная для проектной документации).

=== ER схема (логический уровень)

[plantuml, er-teambridge, svg]
----
@startuml
hide circle
skinparam linetype ortho

entity users {
  * id : uuid <<PK>>
  --
  login : varchar(128) <<UQ>>
  display_name : varchar(255)
  email : varchar(255)
  is_active : boolean
  created_at : timestamptz
  updated_at : timestamptz
}

entity chats {
  * id : uuid <<PK>>
  --
  type : varchar(32)
  title : varchar(255)
  owner_id : uuid <<FK users.id>>
  created_at : timestamptz
}

entity chat_members {
  * chat_id : uuid <<FK chats.id>>
  * user_id : uuid <<FK users.id>>
  --
  role : varchar(32)
  joined_at : timestamptz
  muted_until : timestamptz
}

entity messages {
  * id : uuid <<PK>>
  --
  chat_id : uuid <<FK chats.id>>
  sender_id : uuid <<FK users.id>>
  client_message_id : uuid
  text : text
  edited_at : timestamptz
  deleted_at : timestamptz
  created_at : timestamptz
}

entity files {
  * id : uuid <<PK>>
  --
  uploader_id : uuid <<FK users.id>>
  object_key : varchar(1024)
  filename : varchar(255)
  content_type : varchar(128)
  size_bytes : bigint
  checksum_sha256 : varchar(64)
  created_at : timestamptz
}

entity message_attachments {
  * message_id : uuid <<FK messages.id>>
  * file_id : uuid <<FK files.id>>
  --
  created_at : timestamptz
}

entity notification_settings {
  * user_id : uuid <<PK/FK users.id>>
  --
  mode : varchar(32)
  mute_all : boolean
  mention_only : boolean
  quiet_hours_from : time
  quiet_hours_to : time
  updated_at : timestamptz
}

entity outbox_events {
  * id : uuid <<PK>>
  --
  aggregate_type : varchar(64)
  aggregate_id : uuid
  event_type : varchar(64)
  payload_json : jsonb
  published_at : timestamptz
  created_at : timestamptz
}

users ||--o{ chat_members
chats ||--o{ chat_members
chats ||--o{ messages
users ||--o{ messages
messages ||--o{ message_attachments
files ||--o{ message_attachments
users ||--|| notification_settings
messages ||--o{ outbox_events

@enduml
----

=== DDL ориентир (PostgreSQL)

[source,sql]
----
create table users (
  id uuid primary key,
  login varchar(128) not null unique,
  display_name varchar(255) not null,
  email varchar(255) not null,
  is_active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table chats (
  id uuid primary key,
  type varchar(32) not null, -- direct | group | channel
  title varchar(255) not null,
  owner_id uuid references users(id),
  created_at timestamptz not null default now()
);

create table chat_members (
  chat_id uuid not null references chats(id) on delete cascade,
  user_id uuid not null references users(id) on delete cascade,
  role varchar(32) not null default 'member',
  joined_at timestamptz not null default now(),
  muted_until timestamptz,
  primary key (chat_id, user_id)
);

create table messages (
  id uuid primary key,
  chat_id uuid not null references chats(id) on delete cascade,
  sender_id uuid not null references users(id),
  client_message_id uuid,
  text text not null,
  edited_at timestamptz,
  deleted_at timestamptz,
  created_at timestamptz not null default now(),
  unique (chat_id, sender_id, client_message_id)
);
----

=== Индексы для целевых запросов

|===
|Сущность |Индекс |Назначение

|messages
|`(chat_id, created_at desc)`
|Быстрая пагинация истории чата

|messages
|`(sender_id, created_at desc)`
|История активности пользователя

|chat_members
|`(user_id)`
|Список чатов пользователя

|files
|`(uploader_id, created_at desc)`
|Поиск файлов пользователя

|outbox_events
|`(published_at, created_at)`
|Публикация событий в Kafka и диагностика lag
|===

=== KeyDB/Redis ключи (операционная схема)

|===
|Ключ |Формат |TTL |Назначение

|Presence
|`presence:{user_id} -> online|offline|background`
|60-120 сек
|Статус пользователя

|WS session
|`ws:session:{session_id} -> {user_id, gateway_node}`
|30-60 мин
|Маршрутизация realtime доставки

|User sessions
|`ws:user:{user_id} -> set(session_id)`
|30-60 мин
|Все активные сессии пользователя
|===

=== Ограничения и консистентность

* Источник истины по сообщениям - таблица `messages` в PostgreSQL
* Kafka события публикуются через outbox pattern для устранения рассинхронизации DB и broker
* Доставка realtime может быть повторной (`at least once`) - клиент обязан дедуплицировать по `message_id`
