import {
  I as I2,
  z as z2
} from "./chunk-KSEYCE6T.js";
import {
  C,
  D,
  De as De2,
  Dt,
  Ee as Ee2,
  F as F2,
  N,
  Ne,
  O as O2,
  Oe as Oe2,
  P,
  Q,
  Te as Te2,
  Ze,
  g,
  k as k2,
  ke
} from "./chunk-T7M7PHNW.js";
import {
  $,
  B,
  Ce,
  De,
  E,
  Ee,
  F,
  I,
  Ke,
  Me,
  O,
  Oe,
  R,
  T,
  Te,
  U,
  V,
  W,
  Y,
  _,
  _2,
  _e,
  ae,
  dt,
  et,
  ge,
  k,
  le,
  le2,
  lt,
  me2 as me,
  nt,
  oe2 as oe,
  p,
  pe,
  rt,
  s,
  se,
  ue,
  ue2,
  x2 as x,
  xe,
  xe2,
  z
} from "./chunk-WDWU47US.js";

// node_modules/@likec4/core/dist/_chunks/model.mjs
var F3 = class {
  Aux;
  id;
  _literalId;
  hierarchyLevel;
  imported;
  constructor(e6, t) {
    this.$model = e6, this.$element = t, this.id = this.$element.id, this._literalId = this.$element.id;
    let [n, r] = k2(this.id);
    n ? (this.imported = { from: n, fqn: r }, this.hierarchyLevel = V(r)) : (this.imported = null, this.hierarchyLevel = V(this.id));
  }
  get name() {
    return I(this.id);
  }
  get parent() {
    return this.$model.parent(this);
  }
  get kind() {
    return this.$element.kind;
  }
  get shape() {
    return this.style.shape;
  }
  get color() {
    return this.style.color;
  }
  get icon() {
    return this.style.icon ?? null;
  }
  get tags() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`tags`), () => lt([...this.$element.tags ?? [], ...this.$model.specification.elements[this.$element.kind]?.tags ?? []]));
  }
  get metadata() {
    return this.$element.metadata ?? {};
  }
  get title() {
    return this.$element.title;
  }
  get hasSummary() {
    return !!this.$element.summary && !!this.$element.description && !ge(this.$element.summary, this.$element.description);
  }
  get summary() {
    return Dt.memoize(this, `summary`, Ee2(this.$element));
  }
  get description() {
    return Dt.memoize(this, `description`, De2(this.$element));
  }
  get technology() {
    return this.$element.technology ?? null;
  }
  get links() {
    return this.$element.links ?? [];
  }
  get defaultView() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`defaultView`), () => me(this.scopedViews()) ?? null);
  }
  get isRoot() {
    return this.parent === null;
  }
  get style() {
    return xe2(this, `style`, () => Te2({ shape: this.$model.$styles.defaults.shape, color: this.$model.$styles.defaults.color, border: this.$model.$styles.defaults.border, opacity: this.$model.$styles.defaults.opacity, size: this.$model.$styles.defaults.size, padding: this.$model.$styles.defaults.padding, textSize: this.$model.$styles.defaults.text, iconPosition: this.$model.$styles.defaults.iconPosition, ...this.$element.style }));
  }
  get projectId() {
    return this.imported?.from ?? this.$model.projectId;
  }
  isAncestorOf(e6) {
    return R(this, e6);
  }
  isDescendantOf(e6) {
    return R(e6, this);
  }
  ancestors() {
    return this.$model.ancestors(this);
  }
  commonAncestor(e6) {
    let t = U(this.id, e6.id);
    return t ? this.$model.element(t) : null;
  }
  children() {
    return this.$model.children(this);
  }
  descendants(e6) {
    return e6 ? Y([...this.$model.descendants(this)], e6)[Symbol.iterator]() : this.$model.descendants(this);
  }
  siblings() {
    return this.$model.siblings(this);
  }
  *ascendingSiblings() {
    yield* this.siblings();
    for (let e6 of this.ancestors()) yield* e6.siblings();
  }
  *descendingSiblings() {
    for (let e6 of [...this.ancestors()].reverse()) yield* e6.siblings();
    yield* this.siblings();
  }
  incoming(e6 = `all`) {
    return this.$model.incoming(this, e6);
  }
  *incomers(e6 = `all`) {
    let t = /* @__PURE__ */ new Set();
    for (let n of this.incoming(e6)) t.has(n.source.id) || (t.add(n.source.id), yield n.source);
  }
  outgoing(e6 = `all`) {
    return this.$model.outgoing(this, e6);
  }
  *outgoers(e6 = `all`) {
    let t = /* @__PURE__ */ new Set();
    for (let n of this.outgoing(e6)) t.has(n.target.id) || (t.add(n.target.id), yield n.target);
  }
  get allOutgoing() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`allOutgoing`), () => new Set(this.outgoing()));
  }
  get allIncoming() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`allIncoming`), () => new Set(this.incoming()));
  }
  views() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`views`), () => {
      let e6 = /* @__PURE__ */ new Set();
      for (let t of this.$model.views()) t.includesElement(this.id) && e6.add(t);
      return e6;
    });
  }
  scopedViews() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`scopedViews`), () => {
      let e6 = /* @__PURE__ */ new Set();
      for (let t of this.$model.views()) t.isScopedElementView() && t.viewOf.id === this.id && e6.add(t);
      return e6;
    });
  }
  isDeployed() {
    return xe(me(this.deployments()));
  }
  deployments() {
    return this.$model.deployment.instancesOf(this);
  }
  hasMetadata() {
    return !!this.$element.metadata && !le(this.$element.metadata);
  }
  getMetadata(e6) {
    return e6 ? this.$element.metadata?.[e6] : this.$element.metadata ?? {};
  }
  isTagged(e6) {
    return this.tags.includes(e6);
  }
};
var Oe3 = class {
  Aux;
  get style() {
    return xe2(this, `style`, () => Te2({ shape: this.$model.$styles.defaults.shape, color: this.$model.$styles.defaults.color, border: this.$model.$styles.defaults.border, opacity: this.$model.$styles.defaults.opacity, size: this.$model.$styles.defaults.size, padding: this.$model.$styles.defaults.padding, textSize: this.$model.$styles.defaults.text, iconPosition: this.$model.$styles.defaults.iconPosition, ...this.$node.style }));
  }
  get name() {
    return I(this.id);
  }
  get shape() {
    return this.style.shape;
  }
  get color() {
    return this.style.color;
  }
  get icon() {
    return this.style.icon ?? null;
  }
  get summary() {
    return Dt.memoize(this, `summary`, Ee2(this.$node));
  }
  get description() {
    return Dt.memoize(this, `description`, De2(this.$node));
  }
  get technology() {
    return this.$node.technology ?? null;
  }
  get links() {
    return this.$node.links ?? [];
  }
  ancestors() {
    return this.$model.ancestors(this);
  }
  commonAncestor(e6) {
    let t = U(this.id, e6.id);
    return t ? this.$model.node(t) : null;
  }
  siblings() {
    return this.$model.siblings(this);
  }
  isSibling(e6) {
    return this.parent === e6.parent;
  }
  *ascendingSiblings() {
    yield* this.siblings();
    for (let e6 of this.ancestors()) yield* e6.siblings();
  }
  *descendingSiblings() {
    for (let e6 of [...this.ancestors()].reverse()) yield* e6.siblings();
    yield* this.siblings();
  }
  incoming(e6 = `all`) {
    return this.$model.incoming(this, e6);
  }
  outgoing(e6 = `all`) {
    return this.$model.outgoing(this, e6);
  }
  *incomers(e6 = `all`) {
    let t = /* @__PURE__ */ new Set();
    for (let n of this.incoming(e6)) t.has(n.source.id) || (t.add(n.source.id), yield n.source);
  }
  *outgoers(e6 = `all`) {
    let t = /* @__PURE__ */ new Set();
    for (let n of this.outgoing(e6)) t.has(n.target.id) || (t.add(n.target.id), yield n.target);
  }
  *views() {
    for (let e6 of this.$model.views()) e6._type === `deployment` && e6.includesDeployment(this.id) && (yield e6);
  }
  isDeploymentNode() {
    return false;
  }
  isInstance() {
    return false;
  }
  get allOutgoing() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`allOutgoing`), () => z3.from(new Set(this.outgoingModelRelationships()), new Set(this.outgoing())));
  }
  get allIncoming() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`allIncoming`), () => z3.from(new Set(this.incomingModelRelationships()), new Set(this.incoming())));
  }
  hasMetadata() {
    return !!this.$node.metadata && !le(this.$node.metadata);
  }
  getMetadata(e6) {
    return e6 ? this.$node.metadata?.[e6] : this.$node.metadata ?? {};
  }
  isTagged(e6) {
    return this.tags.includes(e6);
  }
};
var I3 = class extends Oe3 {
  id;
  _literalId;
  title;
  hierarchyLevel;
  constructor(e6, t) {
    super(), this.$model = e6, this.$node = t, this.id = t.id, this._literalId = t.id, this.title = t.title, this.hierarchyLevel = V(t.id);
  }
  get parent() {
    return this.$model.parent(this);
  }
  get kind() {
    return this.$node.kind;
  }
  get metadata() {
    return this.$node.metadata ?? {};
  }
  get tags() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`tags`), () => lt([...this.$node.tags ?? [], ...this.$model.$model.specification.deployments[this.kind]?.tags ?? []]));
  }
  children() {
    return this.$model.children(this);
  }
  descendants(e6 = `desc`) {
    return this.$model.descendants(this, e6);
  }
  isDeploymentNode() {
    return true;
  }
  *instances() {
    for (let e6 of this.descendants(`desc`)) e6.isInstance() && (yield e6);
  }
  onlyOneInstance() {
    let e6 = this.children();
    if (e6.size !== 1) return null;
    let t = me(e6);
    return t ? t.isInstance() ? t : t.onlyOneInstance() : null;
  }
  _relationshipsFromInstances = null;
  relationshipsFromInstances() {
    if (this._relationshipsFromInstances) return this._relationshipsFromInstances;
    let { outgoing: e6, incoming: t } = this._relationshipsFromInstances = { outgoing: /* @__PURE__ */ new Set(), incoming: /* @__PURE__ */ new Set() };
    for (let n of this.instances()) {
      for (let t2 of n.element.outgoing()) e6.add(t2);
      for (let e7 of n.element.incoming()) t.add(e7);
    }
    return this._relationshipsFromInstances;
  }
  outgoingModelRelationships() {
    return this.relationshipsFromInstances().outgoing.values();
  }
  incomingModelRelationships() {
    return this.relationshipsFromInstances().incoming.values();
  }
  internalModelRelationships() {
    let { outgoing: e6, incoming: t } = this.relationshipsFromInstances();
    return $(t, e6);
  }
};
var L = class extends Oe3 {
  id;
  _literalId;
  title;
  hierarchyLevel;
  constructor(e6, t, n) {
    super(), this.$model = e6, this.$instance = t, this.element = n, this.id = t.id, this._literalId = t.id, this.title = t.title ?? n.title, this.hierarchyLevel = V(t.id);
  }
  get $node() {
    return this.$instance;
  }
  get parent() {
    return ae(this.$model.parent(this), `Parent of ${this.id} not found`);
  }
  get style() {
    return xe2(this, `style`, () => Te2({ shape: this.$model.$styles.defaults.shape, color: this.$model.$styles.defaults.color, border: this.$model.$styles.defaults.border, opacity: this.$model.$styles.defaults.opacity, size: this.$model.$styles.defaults.size, padding: this.$model.$styles.defaults.padding, textSize: this.$model.$styles.defaults.text, iconPosition: this.$model.$styles.defaults.iconPosition, ...this.element.$element.style, ...this.$instance.style }));
  }
  get tags() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`tags`), () => lt([...this.$instance.tags ?? [], ...this.element.tags]));
  }
  get kind() {
    return this.element.kind;
  }
  get metadata() {
    return this.$instance.metadata ?? this.element.metadata ?? {};
  }
  get summary() {
    return Dt.memoize(this, `summary`, Ee2(this.$instance) ?? Ee2(this.element.$element));
  }
  get description() {
    return Dt.memoize(this, `description`, De2(this.$instance) ?? De2(this.element.$element));
  }
  get technology() {
    return this.$instance.technology ?? this.element.technology ?? null;
  }
  get links() {
    return this.$instance.links ?? this.element.links;
  }
  isInstance() {
    return true;
  }
  outgoingModelRelationships() {
    return this.element.outgoing();
  }
  incomingModelRelationships() {
    return this.element.incoming();
  }
  *views() {
    for (let e6 of this.$model.views()) if (e6._type === `deployment`) {
      if (e6.includesDeployment(this.id)) {
        yield e6;
        continue;
      }
      e6.includesDeployment(this.parent.id) && this.parent.onlyOneInstance() && (yield e6);
    }
  }
};
var ke2 = class {
  constructor(e6, t) {
    this.instance = e6, this.element = t;
  }
  get id() {
    return this.instance.id;
  }
  get _literalId() {
    return this.instance.id;
  }
  get style() {
    return xe2(this, `style `, () => ({ shape: this.element.shape, color: this.element.color, ...this.element.$element.style }));
  }
  get shape() {
    return this.element.shape;
  }
  get color() {
    return this.element.color;
  }
  get title() {
    return this.element.title;
  }
  get summary() {
    return this.element.summary;
  }
  get description() {
    return this.element.description;
  }
  get technology() {
    return this.element.technology;
  }
  isDeploymentNode() {
    return false;
  }
  isInstance() {
    return false;
  }
};
var R2 = class {
  boundary;
  source;
  target;
  constructor(e6, t) {
    this.$model = e6, this.$relationship = t, this.source = e6.deploymentRef(t.source), this.target = e6.deploymentRef(t.target);
    let n = U(this.source.id, this.target.id);
    this.boundary = n ? this.$model.node(n) : null;
  }
  get id() {
    return this.$relationship.id;
  }
  get expression() {
    return `${this.source.id} -> ${this.target.id}`;
  }
  get title() {
    return xe(this.$relationship.title) ? this.$relationship.title : null;
  }
  get technology() {
    return ue(this.$relationship.technology) ? (this.kind && this.$model.specification.relationships[this.kind])?.technology ?? null : this.$relationship.technology;
  }
  get hasSummary() {
    return !!this.$relationship.summary && !!this.$relationship.description && !ge(this.$relationship.summary, this.$relationship.description);
  }
  get summary() {
    return Dt.memoize(this, `summary`, Ee2(this.$relationship));
  }
  get description() {
    return Dt.memoize(this, `description`, De2(this.$relationship));
  }
  get tags() {
    return this.$relationship.tags ?? [];
  }
  get kind() {
    return this.$relationship.kind ?? null;
  }
  get metadata() {
    return this.$relationship.metadata ?? {};
  }
  get navigateTo() {
    return this.$relationship.navigateTo ? this.$model.$model.view(this.$relationship.navigateTo) : null;
  }
  get links() {
    return this.$relationship.links ?? [];
  }
  get color() {
    return this.$relationship.color ?? this.$model.$styles.defaults.relationship.color;
  }
  get line() {
    return this.$relationship.line ?? this.$model.$styles.defaults.relationship.line;
  }
  get head() {
    return this.$relationship.head ?? this.$model.$styles.defaults.relationship.arrow;
  }
  get tail() {
    return this.$relationship.tail;
  }
  *views() {
    for (let e6 of this.$model.views()) e6.includesRelation(this.id) && (yield e6);
  }
  isDeploymentRelation() {
    return true;
  }
  isModelRelation() {
    return false;
  }
  hasMetadata() {
    return !!this.$relationship.metadata && !le(this.$relationship.metadata);
  }
  getMetadata(e6) {
    return e6 ? this.$relationship.metadata?.[e6] : this.$relationship.metadata ?? {};
  }
  isTagged(e6) {
    return this.tags.includes(e6);
  }
};
var z3 = class e {
  static empty() {
    return new e();
  }
  static from(t, n) {
    return new e(new Set(t), new Set(n));
  }
  constructor(e6 = /* @__PURE__ */ new Set(), t = /* @__PURE__ */ new Set()) {
    this.model = e6, this.deployment = t;
  }
  get isEmpty() {
    return this.model.size === 0 && this.deployment.size === 0;
  }
  get nonEmpty() {
    return this.model.size > 0 || this.deployment.size > 0;
  }
  get size() {
    return this.model.size + this.deployment.size;
  }
  intersect(t) {
    return e.from($(this.model, t.model), $(this.deployment, t.deployment));
  }
  difference(t) {
    return e.from(De(this.model, t.model), De(this.deployment, t.deployment));
  }
  union(t) {
    return e.from(Ee(this.model, t.model), Ee(this.deployment, t.deployment));
  }
};
var B2 = (e6) => typeof e6 == `string` ? e6 : e6.id;
var Ae = `/`;
var V2 = (e6) => {
  if (x(!e6.includes(`
`), `View title cannot contain newlines`), e6.includes(`/`)) {
    let t = e6.split(`/`).map((e7) => e7.trim()).filter((e7) => e7.length > 0);
    return _(t, 1) ? t : [``];
  }
  return [e6.trim()];
};
var H = (e6) => V2(e6).join(`/`);
var je = (e6) => {
  let t = V2(e6);
  return _(t, 2) ? t.slice(0, -1).join(`/`) : null;
};
var Me2 = (e6) => e6.includes(`/`) ? V2(e6).pop() ?? e6 : e6.trim();
var Ne2 = class {
  #e = /* @__PURE__ */ new Map();
  #t = /* @__PURE__ */ new Map();
  #n = new _2.default(() => /* @__PURE__ */ new Set());
  #r = new _2.default(() => /* @__PURE__ */ new Set());
  #i = /* @__PURE__ */ new Set();
  #a = /* @__PURE__ */ new Map();
  #o = new _2.default(() => /* @__PURE__ */ new Set());
  #s = new _2.default(() => /* @__PURE__ */ new Set());
  #c = new _2.default(() => /* @__PURE__ */ new Set());
  #l = new _2.default(() => /* @__PURE__ */ new Set());
  #u = /* @__PURE__ */ new Map();
  $deployments;
  constructor(e6) {
    this.$model = e6;
    let t = this.$deployments = e6.$data.deployments, n = dt(t.elements);
    for (let e7 of oe(n)) {
      let t2 = this.addElement(e7);
      for (let e8 of t2.tags) this.#l.get(e8).add(t2);
      t2.isInstance() && this.#r.get(t2.element.id).add(t2);
    }
    for (let e7 of dt(t.relations)) {
      let t2 = this.addRelation(e7);
      for (let e8 of t2.tags) this.#l.get(e8).add(t2);
    }
  }
  get $styles() {
    return this.$model.$styles;
  }
  get projectId() {
    return this.$model.projectId;
  }
  get project() {
    return this.$model.project;
  }
  get specification() {
    return this.$model.specification;
  }
  element(e6) {
    if (e6 instanceof I3 || e6 instanceof L) return e6;
    let t = B2(e6);
    return ae(this.#e.get(t), `Element ${t} not found`);
  }
  findElement(e6) {
    return this.#e.get(e6) ?? null;
  }
  node(e6) {
    let t = this.element(e6);
    return x(t.isDeploymentNode(), `Element ${t.id} is not a deployment node`), t;
  }
  findNode(e6) {
    let t = this.findElement(e6);
    return t ? (x(t.isDeploymentNode(), `Element ${t?.id} is not a deployment node`), t) : null;
  }
  instance(e6) {
    let t = this.element(e6);
    return x(t.isInstance(), `Element ${t.id} is not a deployed instance`), t;
  }
  findInstance(e6) {
    let t = this.findElement(e6);
    return t ? (x(t.isInstance(), `Element ${t?.id} is not a deployed instance`), t) : null;
  }
  roots() {
    return this.#i.values();
  }
  elements() {
    return this.#e.values();
  }
  *nodes() {
    for (let e6 of this.#e.values()) e6.isDeploymentNode() && (yield e6);
  }
  *nodesOfKind(e6) {
    for (let t of this.#e.values()) t.isDeploymentNode() && t.kind === e6 && (yield t);
  }
  *instances() {
    for (let e6 of this.#e.values()) e6.isInstance() && (yield e6);
  }
  *instancesOf(e6) {
    let t = B2(e6), n = this.#r.get(t);
    n && (yield* n);
  }
  deploymentRef(e6) {
    if (F2.isInsideInstanceRef(e6)) {
      let { deployment: t, element: n } = e6;
      return se(this.#u, `${t}@${n}`, () => new ke2(this.instance(t), this.$model.element(n)));
    }
    return this.element(e6.deployment);
  }
  relationships() {
    return this.#a.values();
  }
  relationship(e6) {
    let t = B2(e6);
    return ae(this.#a.get(t), `DeploymentRelationModel ${t} not found`);
  }
  findRelationship(e6) {
    return this.#a.get(e6) ?? null;
  }
  *views() {
    for (let e6 of this.$model.views()) e6.isDeploymentView() && (yield e6);
  }
  parent(e6) {
    let t = B2(e6);
    return this.#t.get(t) || null;
  }
  children(e6) {
    let t = B2(e6);
    return this.#n.get(t);
  }
  *siblings(e6) {
    let t = B2(e6), n = this.parent(e6)?.children() ?? this.roots();
    for (let e7 of n) e7.id !== t && (yield e7);
  }
  *ancestors(e6) {
    let t = B2(e6), n;
    for (; n = this.#t.get(t); ) yield n, t = n.id;
  }
  *descendants(e6, t = `desc`) {
    for (let n of this.children(e6)) t === `asc` ? (yield n, yield* this.descendants(n.id)) : (yield* this.descendants(n.id), yield n);
  }
  *incoming(e6, t = `all`) {
    let n = B2(e6);
    for (let e7 of this.#o.get(n)) switch (true) {
      case t === `all`:
      case (t === `direct` && e7.target.id === n):
      case (t === `to-descendants` && e7.target.id !== n):
        yield e7;
        break;
    }
  }
  *outgoing(e6, t = `all`) {
    let n = B2(e6);
    for (let e7 of this.#s.get(n)) switch (true) {
      case t === `all`:
      case (t === `direct` && e7.source.id === n):
      case (t === `from-descendants` && e7.source.id !== n):
        yield e7;
        break;
    }
  }
  addElement(e6) {
    if (this.#e.has(e6.id)) throw Error(`Element ${e6.id} already exists`);
    let t = Ne(e6) ? new I3(this, Object.freeze(e6)) : new L(this, Object.freeze(e6), this.$model.element(e6.element));
    this.#e.set(t.id, t);
    let n = F(t.id);
    return n ? (x(this.#e.has(n), `Parent ${n} of ${t.id} not found`), this.#t.set(t.id, this.node(n)), this.#n.get(n).add(t)) : (x(t.isDeploymentNode(), `Root element ${t.id} is not a deployment node`), this.#i.add(t)), t;
  }
  addRelation(e6) {
    if (this.#a.has(e6.id)) throw Error(`Relation ${e6.id} already exists`);
    let t = new R2(this, Object.freeze(e6));
    this.#a.set(t.id, t), this.#o.get(t.target.id).add(t), this.#s.get(t.source.id).add(t);
    let n = t.boundary?.id ?? null;
    if (n) for (let e7 of [n, ...W(n)]) this.#c.get(e7).add(t);
    for (let e7 of W(t.source.id)) {
      if (e7 === n) break;
      this.#s.get(e7).add(t);
    }
    for (let e7 of W(t.target.id)) {
      if (e7 === n) break;
      this.#o.get(e7).add(t);
    }
    return t;
  }
};
var U2 = class {
  source;
  target;
  boundary;
  constructor(e6, t) {
    this.model = e6, this.$relationship = t, this.source = e6.element(F2.flatten(t.source)), this.target = e6.element(F2.flatten(t.target));
    let n = U(this.source.id, this.target.id);
    this.boundary = n ? this.model.element(n) : null;
  }
  get id() {
    return this.$relationship.id;
  }
  get expression() {
    return `${this.source.id} -> ${this.target.id}`;
  }
  get title() {
    return ue(this.$relationship.title) ? null : this.$relationship.title;
  }
  get technology() {
    return ue(this.$relationship.technology) ? (this.kind && this.model.specification.relationships[this.kind])?.technology ?? null : this.$relationship.technology;
  }
  get hasSummary() {
    return !!this.$relationship.summary && !!this.$relationship.description && !ge(this.$relationship.summary, this.$relationship.description);
  }
  get summary() {
    return Dt.memoize(this, `summary`, Ee2(this.$relationship));
  }
  get description() {
    return Dt.memoize(this, `description`, De2(this.$relationship));
  }
  get navigateTo() {
    return this.$relationship.navigateTo ? this.model.view(this.$relationship.navigateTo) : null;
  }
  get tags() {
    return this.$relationship.tags ?? [];
  }
  get kind() {
    return this.$relationship.kind ?? null;
  }
  get metadata() {
    return this.$relationship.metadata ?? {};
  }
  get links() {
    return this.$relationship.links ?? [];
  }
  get color() {
    return this.$relationship.color ?? this.model.$styles.defaults.relationship.color;
  }
  get line() {
    return this.$relationship.line ?? this.model.$styles.defaults.relationship.line;
  }
  get head() {
    return this.$relationship.head ?? this.model.$styles.defaults.relationship.arrow;
  }
  get tail() {
    return this.$relationship.tail;
  }
  *views() {
    for (let e6 of this.model.views()) e6.includesRelation(this.id) && (yield e6);
  }
  isDeploymentRelation() {
    return false;
  }
  isModelRelation() {
    return true;
  }
  hasMetadata() {
    return !!this.$relationship.metadata && !le(this.$relationship.metadata);
  }
  getMetadata(e6) {
    return e6 ? this.$relationship.metadata?.[e6] : this.$relationship.metadata ?? {};
  }
  isTagged(e6) {
    return this.tags.includes(e6);
  }
};
var W2 = class {
  Aux;
  $viewModel;
  $view;
  $edge;
  constructor(e6, t, n, r) {
    this.source = n, this.target = r, this.$viewModel = e6, this.$view = e6.$view, this.$edge = t;
  }
  get id() {
    return this.$edge.id;
  }
  get parent() {
    return this.$edge.parent ? this.$viewModel.node(this.$edge.parent) : null;
  }
  get label() {
    return this.$edge.label ?? null;
  }
  get description() {
    return Dt.memoize(this, `description`, this.$edge.description);
  }
  get technology() {
    return this.$edge.technology ?? null;
  }
  hasParent() {
    return this.$edge.parent !== null;
  }
  get tags() {
    return this.$edge.tags ?? [];
  }
  get stepNumber() {
    return this.isStep() ? P(this.id) : null;
  }
  get navigateTo() {
    return this.$edge.navigateTo ? this.$viewModel.$model.view(this.$edge.navigateTo) : null;
  }
  get color() {
    return this.$edge.color;
  }
  get line() {
    return this.$edge.line ?? this.$viewModel.$styles.defaults.relationship.line;
  }
  get head() {
    return this.$edge.head ?? this.$viewModel.$styles.defaults.relationship.arrow;
  }
  get tail() {
    return this.$edge.tail;
  }
  isStep() {
    return N(this.id);
  }
  *relationships(e6) {
    for (let t of this.$edge.relations) if (e6) {
      let n = this.$viewModel.$model.findRelationship(t, e6);
      n && (yield n);
    } else yield this.$viewModel.$model.relationship(t);
  }
  includesRelation(e6) {
    let t = typeof e6 == `string` ? e6 : e6.id;
    return this.$edge.relations.includes(t);
  }
  isTagged(e6) {
    return this.tags.includes(e6);
  }
};
var G = class {
  Aux;
  $viewModel;
  $view;
  $node;
  constructor(e6, t) {
    this.$viewModel = e6, this.$view = e6.$view, this.$node = t;
  }
  get id() {
    return this.$node.id;
  }
  get title() {
    return this.$node.title;
  }
  get kind() {
    return this.$node.kind;
  }
  get description() {
    return Dt.memoize(this, `description`, this.$node.description);
  }
  get technology() {
    return this.$node.technology ?? null;
  }
  get notes() {
    return Dt.memoize(this, `notes`, this.$node.notes);
  }
  get parent() {
    return this.$node.parent ? this.$viewModel.node(this.$node.parent) : null;
  }
  get element() {
    let e6 = this.$node.modelRef;
    return e6 ? this.$viewModel.$model.element(e6) : null;
  }
  get deployment() {
    let e6 = this.$node.deploymentRef;
    return e6 ? this.$viewModel.$model.deployment.element(e6) : null;
  }
  get shape() {
    return this.$node.shape;
  }
  get color() {
    return this.$node.color;
  }
  get icon() {
    return this.$node.icon ?? null;
  }
  get tags() {
    return this.$node.tags;
  }
  get links() {
    return this.$node.links ?? [];
  }
  get navigateTo() {
    return this.$node.navigateTo ? this.$viewModel.$model.view(this.$node.navigateTo) : null;
  }
  get style() {
    return this.$node.style;
  }
  get x() {
    return `x` in this.$node ? this.$node.x : void 0;
  }
  get y() {
    return `y` in this.$node ? this.$node.y : void 0;
  }
  get width() {
    return `width` in this.$node ? this.$node.width : void 0;
  }
  get height() {
    return `height` in this.$node ? this.$node.height : void 0;
  }
  children() {
    return xe2(this, `children`, () => new Set(this.$node.children.map((e6) => this.$viewModel.node(e6))));
  }
  *ancestors() {
    let e6 = this.parent;
    for (; e6; ) yield e6, e6 = e6.parent;
  }
  *siblings() {
    let e6 = this.parent?.children() ?? this.$viewModel.roots();
    for (let t of e6) t.id !== this.id && (yield t);
  }
  *incoming(e6 = `all`) {
    for (let t of this.$node.inEdges) {
      let n = this.$viewModel.edge(t);
      switch (true) {
        case e6 === `all`:
        case (e6 === `direct` && n.target.id === this.id):
        case (e6 === `to-descendants` && n.target.id !== this.id):
          yield n;
          break;
      }
    }
  }
  *incomers(e6 = `all`) {
    let t = /* @__PURE__ */ new Set();
    for (let n of this.incoming(e6)) t.has(n.source.id) || (t.add(n.source.id), yield n.source);
  }
  *outgoing(e6 = `all`) {
    for (let t of this.$node.outEdges) {
      let n = this.$viewModel.edge(t);
      switch (true) {
        case e6 === `all`:
        case (e6 === `direct` && n.source.id === this.id):
        case (e6 === `from-descendants` && n.source.id !== this.id):
          yield n;
          break;
      }
    }
  }
  *outgoers(e6 = `all`) {
    let t = /* @__PURE__ */ new Set();
    for (let n of this.outgoing(e6)) t.has(n.target.id) || (t.add(n.target.id), yield n.target);
  }
  isLayouted() {
    return `width` in this.$node && `height` in this.$node;
  }
  hasChildren() {
    return this.$node.children.length > 0;
  }
  hasParent() {
    return this.$node.parent !== null;
  }
  hasElement() {
    return xe(this.$node.modelRef);
  }
  hasDeployment() {
    return xe(this.$node.deploymentRef);
  }
  hasDeployedInstance() {
    return this.hasElement() && this.hasDeployment();
  }
  isGroup() {
    return C(this.$node);
  }
  isTagged(e6) {
    return this.tags.includes(e6);
  }
};
var K = class {
  Aux;
  #e;
  #t = /* @__PURE__ */ new Set();
  #n = /* @__PURE__ */ new Map();
  #r = /* @__PURE__ */ new Map();
  #i = /* @__PURE__ */ new Set();
  #a = /* @__PURE__ */ new Set();
  #o = /* @__PURE__ */ new Set();
  #s = new _2.default((e6) => /* @__PURE__ */ new Set());
  #c;
  id;
  $model;
  title;
  folder;
  viewPath;
  constructor(e6, t, n, r) {
    this.$model = e6, this.#e = n, this.id = n.id, this.folder = t, this.#c = r;
    for (let e7 of this.#e.nodes) {
      let t2 = new G(this, Object.freeze(e7));
      this.#n.set(e7.id, t2), e7.parent || this.#t.add(t2), e7.deploymentRef && this.#a.add(e7.deploymentRef), e7.modelRef && this.#i.add(e7.modelRef);
      for (let e8 of t2.tags) this.#s.get(e8).add(t2);
    }
    for (let e7 of this.#e.edges) {
      let t2 = new W2(this, Object.freeze(e7), this.node(e7.source), this.node(e7.target));
      for (let e8 of t2.tags) this.#s.get(e8).add(t2);
      for (let t3 of e7.relations) this.#o.add(t3);
      this.#r.set(e7.id, t2);
    }
    this.title = n.title ? Me2(n.title) : null, this.viewPath = n.title ? H(n.title) : n.id;
  }
  get $styles() {
    return this.$model.$styles;
  }
  get _type() {
    return this.#e[ke];
  }
  get stage() {
    return this.#e[g];
  }
  get bounds() {
    if (`bounds` in this.#e) return this.#e.bounds;
    if (this.#c) return this.#c.bounds;
    throw Error(`View is not layouted`);
  }
  get titleOrId() {
    return this.title ?? this.viewOf?.title ?? this.id;
  }
  get titleOrUntitled() {
    return this.title ?? `Untitled`;
  }
  get breadcrumbs() {
    return xe2(this, `breadcrumbs`, () => this.folder.isRoot ? [this] : [...this.folder.breadcrumbs, this]);
  }
  get description() {
    return Dt.memoize(this, `description`, this.#e.description);
  }
  get tags() {
    return this.#e.tags ?? [];
  }
  get links() {
    return this.#e.links ?? [];
  }
  get viewOf() {
    if (this.isElementView()) {
      let e6 = this.#e.viewOf;
      return e6 ? this.$model.element(e6) : null;
    }
    return null;
  }
  get mode() {
    return this.isDynamicView() ? this.#e.variant ?? `diagram` : null;
  }
  get includedTags() {
    return [...this.#s.keys()];
  }
  get $view() {
    if (!this.isLayouted() || `drifts` in this.#e) return this.#e;
    let e6 = this.#c;
    return e6 ? xe2(this, `withDriftReasons`, () => z2(this.#e, e6)) : this.#e;
  }
  get $layouted() {
    if (!this.isLayouted()) throw Error(`View is not layouted`);
    return this.$manual ?? this.#e;
  }
  get hasManualLayout() {
    return this.#c !== void 0;
  }
  get hasLayoutDrifts() {
    if (!this.isLayouted()) return false;
    let e6 = this.$manual;
    return !!e6?.drifts && e6.drifts.length > 0;
  }
  get $manual() {
    if (!this.isLayouted()) return null;
    let e6 = this.#c;
    return e6 ? xe2(this, `snapshotWithManualLayout`, () => I2(this.#e, e6)) : null;
  }
  get projectId() {
    return this.$model.projectId;
  }
  roots() {
    return this.#t.values();
  }
  *compounds() {
    for (let e6 of this.#n.values()) e6.hasChildren() && (yield e6);
  }
  node(e6) {
    let t = B2(e6);
    return ae(this.#n.get(t), `Node ${t} not found in view ${this.#e.id}`);
  }
  findNode(e6) {
    return this.#n.get(B2(e6)) ?? null;
  }
  findNodeWithElement(e6) {
    let t = B2(e6);
    return this.#i.has(t) ? ue2(this.#n.values(), (e7) => e7.hasElement() && e7.element.id === t) ?? null : null;
  }
  nodes() {
    return this.#n.values();
  }
  edge(e6) {
    let t = B2(e6);
    return ae(this.#r.get(t), `Edge ${t} not found in view ${this.#e.id}`);
  }
  findEdge(e6) {
    return this.#r.get(B2(e6)) ?? null;
  }
  edges() {
    return this.#r.values();
  }
  *edgesWithRelation(e6) {
    for (let t of this.#r.values()) t.includesRelation(e6) && (yield t);
  }
  *elements() {
    for (let e6 of this.#n.values()) e6.hasElement() && (yield e6);
  }
  isTagged(e6) {
    return this.tags.includes(e6);
  }
  includesElement(e6) {
    return this.#i.has(B2(e6));
  }
  includesDeployment(e6) {
    return this.#a.has(B2(e6));
  }
  includesRelation(e6) {
    return this.#o.has(B2(e6));
  }
  isComputed() {
    return this.#e[g] === `computed`;
  }
  isLayouted() {
    return this.#e[g] === `layouted`;
  }
  isDiagram() {
    return this.#e[g] === `layouted`;
  }
  isElementView() {
    return this.#e[ke] === `element`;
  }
  isScopedElementView() {
    return this.#e[ke] === `element` && xe(this.#e.viewOf);
  }
  isDeploymentView() {
    return this.#e[ke] === `deployment`;
  }
  isDynamicView() {
    return this.#e[ke] === `dynamic`;
  }
};
var q = class e2 {
  $model;
  path;
  title;
  isRoot;
  parentPath;
  defaultViewId;
  constructor(e6, t, r) {
    this.$model = e6, this.path = t.join(`/`), this.isRoot = this.path === ``, this.title = Ce(t), this.isRoot ? this.parentPath = void 0 : this.parentPath = t.slice(0, -1).join(`/`), this.defaultViewId = r;
  }
  get defaultView() {
    return this.defaultViewId ? this.$model.view(this.defaultViewId) : null;
  }
  get breadcrumbs() {
    return x(!this.isRoot, `Root view folder has no breadcrumbs`), xe2(this, `breadcrumbs`, () => {
      let e6 = this.parent;
      return e6 ? e6.isRoot ? [e6, this] : [...e6.breadcrumbs, this] : [this];
    });
  }
  get parent() {
    return x(!this.isRoot, `Root view folder has no parent`), ue(this.parentPath) ? null : this.$model.viewFolder(this.parentPath);
  }
  get children() {
    return this.$model.viewFolderItems(this.path);
  }
  get folders() {
    return xe2(this, `folders`, () => {
      let t = [];
      for (let n of this.children) n instanceof e2 && t.push(n);
      return t;
    });
  }
  get views() {
    return xe2(this, `views`, () => {
      let e6 = [];
      for (let t of this.children) t instanceof K && e6.push(t);
      return e6;
    });
  }
};
var J = class e3 {
  Aux;
  _elements = /* @__PURE__ */ new Map();
  _parents = /* @__PURE__ */ new Map();
  _children = new _2.default(() => /* @__PURE__ */ new Set());
  _rootElements = /* @__PURE__ */ new Set();
  _relations = /* @__PURE__ */ new Map();
  _incoming = new _2.default(() => /* @__PURE__ */ new Set());
  _outgoing = new _2.default(() => /* @__PURE__ */ new Set());
  _internal = new _2.default(() => /* @__PURE__ */ new Set());
  _views = /* @__PURE__ */ new Map();
  _rootViewFolder;
  _viewFolders = /* @__PURE__ */ new Map();
  _viewFolderItems = new _2.default(() => /* @__PURE__ */ new Set());
  _allTags = new _2.default(() => /* @__PURE__ */ new Set());
  static fromParsed(t) {
    return new e3(t);
  }
  static create(t) {
    return new e3(t);
  }
  static fromDump(t) {
    let { _stage: n = `layouted`, projectId: r = `unknown`, project: i, globals: a, imports: o, deployments: s2, views: c, relations: l, elements: ee, specification: u } = t;
    return new e3({ [g]: n, projectId: r, project: i, globals: { predicates: a?.predicates ?? {}, dynamicPredicates: a?.dynamicPredicates ?? {}, styles: a?.styles ?? {} }, imports: o ?? {}, deployments: { elements: s2?.elements ?? {}, relations: s2?.relations ?? {} }, views: c ?? {}, relations: l ?? {}, elements: ee ?? {}, specification: u });
  }
  deployment;
  $data;
  constructor(e6) {
    this.$data = e6;
    for (let [, t] of E(e6.elements)) {
      let e7 = this.addElement(t);
      for (let t2 of e7.tags) this._allTags.get(t2).add(e7);
    }
    for (let [t, n] of E(e6.imports ?? {})) for (let e7 of oe(n)) {
      let n2 = this.addImportedElement(t, e7);
      for (let e8 of n2.tags) this._allTags.get(e8).add(n2);
    }
    for (let t of dt(e6.relations)) {
      let e7 = this.addRelation(t);
      for (let t2 of e7.tags) this._allTags.get(t2).add(e7);
    }
    if (this.deployment = new Ne2(this), Oe2(e6, `computed`) || Oe2(e6, `layouted`)) {
      let t = O(`/`), n = p(dt(e6.views), Te((e7) => ({ view: e7, path: H(e7.title ?? e7.id), folderPath: e7.title && je(e7.title) || `` })), et((e7, n2) => t(e7.folderPath, n2.folderPath))), r = (e7) => {
        let t2 = this._viewFolders.get(e7);
        if (!t2) {
          let r2 = rt(e7, `/`);
          x(_(r2, 1), `View group path "${e7}" must have at least one element`);
          let i;
          i = e7 === `` ? n.find((e8) => e8.view.id === `index`) : n.find((t3) => t3.path === e7), t2 = new q(this, r2, i?.view.id), this._viewFolders.set(e7, t2);
        }
        return t2;
      };
      this._rootViewFolder = r(``);
      for (let { folderPath: e7 } of n) this._viewFolders.has(e7) || rt(e7, `/`).reduce((e8, t2) => {
        let n2 = e8.join(`/`), a = r(le(n2) ? t2 : n2 + `/` + t2);
        return this._viewFolderItems.get(n2).add(a), e8.push(t2), e8;
      }, []);
      for (let { view: t2, folderPath: i } of n) {
        let n2 = new K(this, r(i), t2, e6.manualLayouts?.[t2.id]);
        this._viewFolderItems.get(i).add(n2), this._views.set(t2.id, n2);
        for (let e7 of n2.tags) this._allTags.get(e7).add(n2);
      }
    } else this._rootViewFolder = new q(this, [``], void 0), this._viewFolders.set(this._rootViewFolder.path, this._rootViewFolder);
  }
  get asParsed() {
    return this;
  }
  get asComputed() {
    return this;
  }
  get asLayouted() {
    return this;
  }
  get $styles() {
    return xe2(this, `styles`, () => Ze.from(this.project.styles, this.specification.customColors));
  }
  isParsed() {
    return this.stage === `parsed`;
  }
  isLayouted() {
    return this.stage === `layouted`;
  }
  isComputed() {
    return this.stage === `computed`;
  }
  get $model() {
    return this.$data;
  }
  get stage() {
    return this.$data[g];
  }
  get projectId() {
    return this.$data.projectId ?? `default`;
  }
  get project() {
    return this.$data.project ?? xe2(this, /* @__PURE__ */ Symbol.for(`project`), () => ({ id: this.projectId, styles: {}, manualLayouts: {} }));
  }
  get specification() {
    return this.$data.specification;
  }
  get globals() {
    return xe2(this, /* @__PURE__ */ Symbol.for(`globals`), () => ({ predicates: { ...this.$data.globals?.predicates }, dynamicPredicates: { ...this.$data.globals?.dynamicPredicates }, styles: { ...this.$data.globals?.styles } }));
  }
  element(e6) {
    if (e6 instanceof F3) return e6;
    let t = B2(e6);
    return ae(this._elements.get(t), `Element ${t} not found`);
  }
  findElement(e6) {
    return this._elements.get(B2(e6)) ?? null;
  }
  roots() {
    return this._rootElements.values();
  }
  elements() {
    return this._elements.values();
  }
  relationships() {
    return this._relations.values();
  }
  relationship(e6, t) {
    if (t === `deployment`) return this.deployment.relationship(e6);
    let n = B2(e6), r = this._relations.get(n) ?? null;
    return r || t === `model` ? ae(r, `Model relation ${n} not found`) : ae(this.deployment.findRelationship(n), `No model/deployment relation ${n} not found`);
  }
  findRelationship(e6, t) {
    if (t === `deployment`) return this.deployment.findRelationship(e6);
    let n = this._relations.get(B2(e6)) ?? null;
    return n || t === `model` ? n : this.deployment.findRelationship(e6);
  }
  views() {
    return this._views.values();
  }
  view(e6) {
    let t = B2(e6);
    return ae(this._views.get(t), `View ${t} not found`);
  }
  findView(e6) {
    return this._views.get(e6) ?? null;
  }
  findManualLayout(e6) {
    return `manualLayouts` in this.$data ? this.$data.manualLayouts?.[e6] ?? null : null;
  }
  viewFolder(e6) {
    return ae(this._viewFolders.get(e6), `View folder ${e6} not found`);
  }
  get rootViewFolder() {
    return this._rootViewFolder;
  }
  get hasViewFolders() {
    return this._viewFolders.size > 1;
  }
  viewFolderItems(e6) {
    return x(this._viewFolders.has(e6), `View folder ${e6} not found`), this._viewFolderItems.get(e6);
  }
  parent(e6) {
    let t = B2(e6);
    return this._parents.get(t) || null;
  }
  children(e6) {
    let t = B2(e6);
    return this._children.get(t);
  }
  *siblings(e6) {
    let t = B2(e6), n = this._parents.get(t), r = n ? this._children.get(n.id).values() : this.roots();
    for (let e7 of r) e7.id !== t && (yield e7);
  }
  *ancestors(e6) {
    let t = B2(e6), n;
    for (; n = this._parents.get(t); ) yield n, t = n.id;
  }
  *descendants(e6) {
    for (let t of this.children(e6)) yield t, yield* this.descendants(t.id);
  }
  *incoming(e6, t = `all`) {
    let n = B2(e6);
    for (let e7 of this._incoming.get(n)) switch (true) {
      case t === `all`:
      case (t === `direct` && e7.target.id === n):
      case (t === `to-descendants` && e7.target.id !== n):
        yield e7;
        break;
    }
  }
  *outgoing(e6, t = `all`) {
    let n = B2(e6);
    for (let e7 of this._outgoing.get(n)) switch (true) {
      case t === `all`:
      case (t === `direct` && e7.source.id === n):
      case (t === `from-descendants` && e7.source.id !== n):
        yield e7;
        break;
    }
  }
  get tags() {
    return xe2(this, `tags`, () => et([...this._allTags.keys()], T));
  }
  get tagsSortedByUsage() {
    return xe2(this, `tagsSortedByUsage`, () => p([...this._allTags.entries()], Te(([e6, t]) => ({ tag: e6, count: t.size, tagged: t })), et((e6, t) => T(e6.tag, t.tag)), nt([Ke(`count`), `desc`])));
  }
  findByTag(e6, t) {
    return le2(this._allTags.get(e6), (e7) => t === `elements` ? e7 instanceof F3 : t === `views` ? e7 instanceof K : t === `relationships` ? e7 instanceof U2 : true);
  }
  *elementsOfKind(e6) {
    for (let t of this._elements.values()) t.kind === e6 && (yield t);
  }
  *elementsWhere(e6) {
    let t = Q(e6);
    for (let e7 of this._elements.values()) t(e7) && (yield e7);
  }
  *relationshipsWhere(e6) {
    let t = Q(e6);
    for (let e7 of this._relations.values()) t(e7) && (yield e7);
  }
  addElement(e6) {
    if (this._elements.has(e6.id)) throw Error(`Element ${e6.id} already exists`);
    let t = new F3(this, Object.freeze(e6));
    this._elements.set(t.id, t);
    let n = F(t.id);
    return n ? (x(this._elements.has(n), `Parent ${n} of ${t.id} not found`), this._parents.set(t.id, this.element(n)), this._children.get(n).add(t)) : this._rootElements.add(t), t;
  }
  addImportedElement(e6, t) {
    x(!O2(t.id), `Imported element already has global FQN`);
    let n = D(e6, t.id);
    if (this._elements.has(n)) throw Error(`Element ${n} already exists`);
    let r = new F3(this, Object.freeze({ ...t, id: n }));
    this._elements.set(r.id, r);
    let i = F(r.id);
    for (; i; ) {
      if (i.includes(`.`) && this._elements.has(i)) return this._parents.set(r.id, this.element(i)), this._children.get(i).add(r), r;
      i = F(i);
    }
    return this._rootElements.add(r), r;
  }
  addRelation(e6) {
    if (this._relations.has(e6.id)) throw Error(`Relation ${e6.id} already exists`);
    let t = new U2(this, Object.freeze(e6)), { source: n, target: r } = t;
    this._relations.set(t.id, t), this._incoming.get(r.id).add(t), this._outgoing.get(n.id).add(t);
    let i = U(n.id, r.id);
    if (i) for (let e7 of [i, ...W(i)]) this._internal.get(e7).add(t);
    for (let e7 of W(n.id)) {
      if (e7 === i) break;
      this._outgoing.get(e7).add(t);
    }
    for (let e7 of W(r.id)) {
      if (e7 === i) break;
      this._incoming.get(e7).add(t);
    }
    return t;
  }
};
(function(e6) {
  e6.EMPTY = J.create({ _stage: `computed`, projectId: `default`, project: { id: `default` }, specification: { elements: {}, relationships: {}, deployments: {}, tags: {} }, globals: { predicates: {}, dynamicPredicates: {}, styles: {} }, deployments: { elements: {}, relations: {} }, elements: {}, relations: {}, views: {}, imports: {} });
})(J ||= {});
var Pe;
(function(e6) {
  e6.isInside = (e7) => (t2) => R(e7, t2.source.id) && R(e7, t2.target.id);
  let t = e6.isDirectedBetween = (e7, t2) => (n2) => (n2.source.id === e7 || R(e7, n2.source.id)) && (n2.target.id === t2 || R(t2, n2.target.id));
  e6.isAnyBetween = (e7, n2) => {
    let r2 = t(e7, n2), i = t(n2, e7);
    return (e8) => r2(e8) || i(e8);
  };
  let n = e6.isIncoming = (e7) => (t2) => (t2.target.id === e7 || R(e7, t2.target.id)) && !R(e7, t2.source.id), r = e6.isOutgoing = (e7) => (t2) => (t2.source.id === e7 || R(e7, t2.source.id)) && !R(e7, t2.target.id);
  e6.isAnyInOut = (e7) => {
    let t2 = n(e7), i = r(e7);
    return (e8) => t2(e8) || i(e8);
  };
})(Pe ||= {});
var Y2 = /* @__PURE__ */ Symbol.for(`nodejs.util.inspect.custom`);
var Fe = class e4 {
  id;
  constructor(e6, t, n) {
    this.source = e6, this.target = t, this.relations = n, this.id = Me(`deployment:${e6.id}:${t.id}`);
  }
  get expression() {
    return `${this.source.id} -> ${this.target.id}`;
  }
  _boundary;
  get boundary() {
    return this._boundary ??= this.source.commonAncestor(this.target), this._boundary;
  }
  nonEmpty() {
    return this.relations.nonEmpty;
  }
  [Y2](t, n, r) {
    let i = this.toString();
    return Object.defineProperty(i, `constructor`, { value: e4, enumerable: false }), i;
  }
  toString() {
    let e6 = [...this.relations.model].map((e7) => `    ` + e7.expression);
    e6.length ? e6.unshift(`  model:`) : e6.unshift(`  model: []`);
    let t = [...this.relations.deployment].map((e7) => `    ` + e7.expression);
    return t.length ? t.unshift(`  deployment:`) : t.unshift(`  deployment: []`), [this.expression, ...e6, ...t].join(`
`);
  }
  hasDirectDeploymentRelation() {
    for (let e6 of this.relations.deployment) if (e6.source.id === this.source.id || e6.target.id === this.target.id) return true;
    return false;
  }
  *values() {
    yield* this.relations.model, yield* this.relations.deployment;
  }
  mergeWith(t) {
    return Array.isArray(t) ? t.reduce((e6, t2) => e6.mergeWith(t2), this) : (x(this.source.id === t.source.id, `Cannot merge connections with different sources`), x(this.target.id === t.target.id, `Cannot merge connections with different targets`), new e4(this.source, this.target, this.relations.union(t.relations)));
  }
  difference(t) {
    return new e4(this.source, this.target, this.relations.difference(t.relations));
  }
  intersect(t) {
    return new e4(this.source, this.target, this.relations.intersect(t.relations));
  }
  equals(t) {
    return x(t instanceof e4, `Other should ne DeploymentConnectionModel`), this.id === t.id && this.source.id === t.source.id && this.target.id === t.target.id && Oe(this.relations.model, t.relations.model) && Oe(this.relations.deployment, t.relations.deployment);
  }
  update(t) {
    return t &&= { model: this.relations.model, deployment: this.relations.deployment, ...t }, new e4(this.source, this.target, t ? new z3(t.model ?? /* @__PURE__ */ new Set(), t.deployment ?? /* @__PURE__ */ new Set()) : this.relations);
  }
};
function X(e6, t) {
  if (!t) {
    let t2 = e6;
    return (e7) => X(e7, t2);
  }
  let n = e6.source === t.source, r = e6.target === t.target;
  if (n && r) return false;
  let i = R(t.source.id, e6.source.id), a = R(t.target.id, e6.target.id);
  return i && a || n && a || r && i;
}
function Ie(e6, t) {
  let n = t;
  for (let t2 of e6) X(t2, n) && (n = t2);
  return n === t ? null : n;
}
function Le(e6) {
  let t = [], n = e6.slice(), r;
  for (; r = n.shift(); ) {
    let e7;
    for (; e7 = Ie(n, r); ) {
      let r2 = n.indexOf(e7);
      t.push(n.splice(r2, 1)[0]);
    }
    t.push(r);
  }
  return t;
}
function Re(e6) {
  return e6.boundary?.id ? `.${e6.boundary.id}` : ``;
}
function ze(e6, t) {
  if (!e6 || k(e6)) {
    let t2 = e6 ?? `asc`;
    return (e7) => Be(e7, t2);
  }
  return Be(e6, t ?? `asc`);
}
function Be(e6, t) {
  return p(e6, Te((e7) => ({ id: Re(e7), conn: e7 })), Y(t), Te((e7) => e7.conn));
}
function Ve(e6, t) {
  return e6.filter((e7) => X(t, e7));
}
function He(e6, t) {
  return e6.filter(X(t));
}
function Ue(e6) {
  let t = /* @__PURE__ */ new Map();
  for (let n of e6) {
    let e7 = t.get(n.id);
    e7 ? t.set(n.id, n.mergeWith(e7)) : t.set(n.id, n);
  }
  return [...t.values()];
}
function We(e6, t) {
  let n = new Map([...t].map((e7) => [e7.id, e7]));
  return [...e6].reduce((e7, t2) => {
    let r = n.get(t2.id);
    if (!r) return e7.push(t2), e7;
    let i = t2.difference(r);
    return i.nonEmpty() && e7.push(i), e7;
  }, []);
}
function Z(e6, t) {
  return t ? e6.source === t.source && e6.target === t.target : (t2) => e6.source === t2.source && e6.target === t2.target;
}
function Ge(e6, t) {
  return t ? e6.source === t.source : (t2) => e6.source === t2.source;
}
function Ke2(e6, t) {
  return t ? e6.target === t.target : (t2) => e6.target === t2.target;
}
function qe(e6, t) {
  if (!t) {
    let t2 = e6;
    return (e7) => qe(e7, t2);
  }
  let n = e6;
  return B(n.source, t) && !B(n.target, t);
}
function Je(e6, t) {
  if (!t) {
    let t2 = e6;
    return (e7) => Je(e7, t2);
  }
  let n = e6;
  return B(n.target, t) && !B(n.source, t);
}
function Ye(e6, t) {
  if (!t) {
    let t2 = e6;
    return (e7) => Ye(e7, t2);
  }
  let n = e6;
  return B(n.source, t) !== B(n.target, t);
}
var Q2 = class e5 {
  id;
  constructor(e6, t, n = /* @__PURE__ */ new Set()) {
    this.source = e6, this.target = t, this.relations = n, this.id = Me(`model:${e6.id}:${t.id}`);
  }
  _boundary;
  get boundary() {
    return this._boundary ??= this.source.commonAncestor(this.target);
  }
  get expression() {
    return `${this.source.id} -> ${this.target.id}`;
  }
  get isDirect() {
    return this.nonEmpty() && !this.isImplicit;
  }
  get isImplicit() {
    return this.nonEmpty() && _e(this.relations, pe(Z(this)));
  }
  get directRelations() {
    return new Set(le2(this.relations, Z(this)));
  }
  nonEmpty() {
    return this.relations.size > 0;
  }
  mergeWith(t) {
    return x(this.source.id === t.source.id, `Cannot merge connections with different sources`), x(this.target.id === t.target.id, `Cannot merge connections with different targets`), new e5(this.source, this.target, Ee(this.relations, t.relations));
  }
  difference(t) {
    return new e5(this.source, this.target, De(this.relations, t.relations));
  }
  intersect(t) {
    return x(t instanceof e5, `Cannot intersect connection with different type`), new e5(this.source, this.target, $(this.relations, t.relations));
  }
  equals(t) {
    x(t instanceof e5, `Cannot merge connection with different type`);
    let n = t;
    return this.id === n.id && this.source.id === n.source.id && this.target.id === n.target.id && Oe(this.relations, n.relations);
  }
  update(t) {
    return new e5(this.source, this.target, t);
  }
  [Y2](t, n, r) {
    let i = this.toString();
    return Object.defineProperty(i, `constructor`, { value: e5, enumerable: false }), i;
  }
  toString() {
    return [this.expression, this.relations.size ? `  relations:` : `  relations: [ ]`, ...[...this.relations].map((e6) => `    ` + e6.expression)].join(`
`);
  }
  reversed() {
    return new e5(this.target, this.source);
  }
};
var Xe = s({ findConnection: () => $2, findConnectionsBetween: () => Ze2, findConnectionsWithin: () => Qe });
function $2(e6, t, n = `directed`) {
  if (e6 === t || z(e6, t)) return [];
  let r = e6.allOutgoing.intersect(t.allIncoming), i = r.nonEmpty ? [new Fe(e6, t, r)] : [];
  return n === `directed` ? i : [...i, ...$2(t, e6, `directed`)];
}
function Ze2(e6, t, n = `both`) {
  if (e6.allIncoming.isEmpty && e6.allOutgoing.isEmpty) return [];
  let r = [], i = [];
  for (let a of t) if (e6 !== a) for (let t2 of $2(e6, a, n)) t2.source === e6 ? r.push(t2) : i.push(t2);
  return [...r, ...i];
}
function Qe(e6) {
  return [...e6].reduce((e7, t, n, r) => (n === r.length - 1 || e7.push(...Ze2(t, r.slice(n + 1), `both`)), e7), []);
}
var $e = s({ findConnection: () => et2, findConnectionsBetween: () => tt, findConnectionsWithin: () => nt2 });
function et2(e6, t, n = `directed`) {
  if (e6 === t || z(e6, t)) return [];
  let r = $(e6.allOutgoing, t.allIncoming), i = r.size > 0 ? new Q2(e6, t, r) : null;
  if (n === `directed`) return i ? [i] : [];
  let a = $(e6.allIncoming, t.allOutgoing), o = a.size > 0 ? new Q2(t, e6, a) : null;
  return i && o ? [i, o] : i ? [i] : o ? [o] : [];
}
function tt(e6, t, n = `both`) {
  if (e6.allIncoming.size === 0 && e6.allOutgoing.size === 0) return [];
  let r = [], i = [];
  for (let a of t) if (e6 !== a) for (let t2 of et2(e6, a, n)) t2.source === e6 ? r.push(t2) : i.push(t2);
  return [...r, ...i];
}
function nt2(e6) {
  return [...e6].reduce((e7, t, n, r) => (n === r.length - 1 || e7.push(...tt(t, r.slice(n + 1), `both`)), e7), []);
}
function rt2(e6) {
  return e6 instanceof I3;
}
function it(e6) {
  return e6 instanceof L;
}
function at(e6) {
  return rt2(e6) || it(e6);
}
function ot(e6) {
  return e6 instanceof ke2;
}
function st(e6) {
  return e6 instanceof R2;
}
function ct(e6) {
  return e6 instanceof U2;
}
function lt2(e6) {
  return e6 instanceof F3;
}
function ut(e6) {
  return e6 instanceof K;
}
function dt2(e6) {
  return e6 instanceof G;
}
function ft(e6) {
  return e6 instanceof W2;
}

export {
  F3 as F,
  I3 as I,
  L,
  R2 as R,
  z3 as z,
  Ae,
  H,
  je,
  Me2 as Me,
  Ne2 as Ne,
  U2 as U,
  W2 as W,
  G,
  K,
  q,
  J,
  Pe,
  Fe,
  X,
  Ie,
  Le,
  ze,
  Ve,
  He,
  Ue,
  We,
  Z,
  Ge,
  Ke2 as Ke,
  qe,
  Je,
  Ye,
  Q2 as Q,
  Xe,
  $e,
  rt2 as rt,
  it,
  at,
  ot,
  st,
  ct,
  lt2 as lt,
  ut,
  dt2 as dt,
  ft
};
//# sourceMappingURL=chunk-UGXY2CN6.js.map
